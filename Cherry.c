#include <stdio.h>

int main();
{
    m[10];
    int num;
    int n;
    int sum;
    for (int i = 0; i < 10; i++)       
	{
		printf("Write a number: ");    
		scanf_s("%d", &m[i]);         
	}
    for (int i = 0; i < 10; i++)
    {
        if(m[i-1]*m[i] > m[i-1]*m[i-2]
        {
            num = i;
            n = i-1;
        }
    }

    	for (int i = 0; i < 10; i++)       
	{
		printf("%d ", m[i]);           
	}
    printf("Mult %d\n", sum);
}
