#include <stdio.h>
#include <stdlib.h>
#include <conio.h>

int StringLength(int);
char* MemoryAllocation(int);
void Input(char*, int);
char* GetString(char*, int);
char* Calculations(char*, char*, int);
void Output(char*);