#include <stdio.h>
#include <stdlib.h>
#include <conio.h>

char* PointerMemoryAllocation();                    // Выделяет память под указатель
void InputWithDynamicMemoryAllocation(char*);       // Выделяет динамично память по длинне текста
int StringLength(char*);                            // Находит длинну текста
void Calculations(char*, int);                      // Вычисления
